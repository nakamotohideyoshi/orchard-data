// describe('SQLite', () => {
//   it('should load sqlite', () => {
//     var sqlite3 = require('sqlite3').verbose()
//     expect(sqlite3).to.be.an('object')
//   })

// //   it('should create a database', () => {
// //     var sqlite3 = require('sqlite3').verbose()
// //     var db = new sqlite3.Database(':memory:')
// //     expect(db).to.be.an('object')
// //   })
// })
